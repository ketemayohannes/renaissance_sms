CREATE TABLE "grade_components" ("id" integer primary key autoincrement not null, "grade_level_subject_id" integer, "term_id" integer not null, "name" varchar not null, "weight" numeric not null, "max_score" numeric not null default ('100'), "sort_order" integer not null default ('0'), "created_at" datetime, "updated_at" datetime, "assessment_type_id" integer, "academic_year_id" integer, "grade_level_id" integer, "subject_id" integer, "order" integer not null default ('0'), "is_active" tinyint(1) not null default ('1'), foreign key("subject_id") references subjects("id") on delete cascade on update no action, foreign key("grade_level_id") references grade_levels("id") on delete cascade on update no action, foreign key("academic_year_id") references academic_years("id") on delete cascade on update no action, foreign key("assessment_type_id") references assessment_types("id") on delete cascade on update no action, foreign key("grade_level_subject_id") references grade_level_subjects("id") on delete cascade on update no action, foreign key("term_id") references terms("id") on delete cascade on update no action)